function updatePattern() {\n    console.log('Pattern updated');\n    const patternType = document.getElementById('patternSelect')?.value || 'pyramid';\n    const size = parseInt(document.getElementById('sizeInput')?.value) || 5;\n    \n    let pattern = '';\n    switch (patternType) {\n        case 'square':\n            pattern = drawSquare(size);\n            break;\n        case 'triangle':\n            pattern = drawTriangle(size);\n            break;\n        case 'diamond':\n            pattern = drawDiamond(size);\n            break;\n        case 'reverse':\n            pattern = drawReverseTriangle(size);\n            break;\n        case 'pyramid':\n        default:\n            pattern = drawPyramid(size);\n    }\n    \n    displayPattern(pattern);\n}\n\nfunction displayPattern(pattern) {\n    const output = document.getElementById('patternOutput');\n    if (output) {\n        output.textContent = pattern;\n        output.style.whiteSpace = 'pre';\n        output.style.fontFamily = 'monospace';\n    }\n}\n\nfunction increaseSize() {\n    const sizeInput = document.getElementById('sizeInput');\n    if (sizeInput) {\n        sizeInput.value = Math.min(20, parseInt(sizeInput.value) + 1);\n        updatePattern();\n    }\n}\n\nfunction decreaseSize() {\n    const sizeInput = document.getElementById('sizeInput');\n    if (sizeInput) {\n        sizeInput.value = Math.max(1, parseInt(sizeInput.value) - 1);\n        updatePattern();\n    }\n}